# Archive

This folder holds legacy or presentation-only artifacts that are not part of the current primary workflow.

Current contents:

- `demo_app.py`
  - an older standalone Streamlit demo that renders sample data without using the live PostgreSQL-backed pipeline

The active application entrypoint is:

- `streamlit_app.py`

The archive exists to keep the project root cleaner without permanently deleting older artifacts that may still be useful for reference.
